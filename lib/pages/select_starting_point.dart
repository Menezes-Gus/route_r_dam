import 'package:flutter/material.dart';

class SelectStartingPoint extends StatelessWidget {
  const SelectStartingPoint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
