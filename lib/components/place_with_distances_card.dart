import 'package:flutter/material.dart';

class PlaceWithDistancesCard extends StatelessWidget {
  const PlaceWithDistancesCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
